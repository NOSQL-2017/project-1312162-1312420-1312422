<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:page_title) %> | MyStatus</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style>
      .navbar {
      margin-bottom: 0;
      border-radius: 0;
    }
    </style>
  </head>

  <body>
  <nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
      <a class="navbar-brand" href="#">Portfolio</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to 'Home', root_path, class: "nav-item nav-link " %></li>
        <li><%= link_to 'About', about_path, class: "nav-item nav-link #{yield(:about_active)}" %></li>
        <li><%= link_to 'Contact', contact_path, class: "nav-item nav-link #{yield(:contact_active)}" %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
      <% if(session[:current_user_id] == nil)%>
      <li><%= link_to 'Register',register_path, class: "btn btn-outline-success pull-xs-right", style: "margin-left:10px"%></li>
      <li><%= link_to 'login', login_path, class: "btn btn-outline-success pull-xs-right", style: "margin-left:10px"%></li>  
      <% else %>
      <li>
        <% @User = User.find_by(id: session[:current_user_id]) %>
        <%= link_to  info_path do %>
        <%=@User.username%>
        <% end %>
      </li>
        <li><%= link_to 'logout',logout_path, class: "btn btn-outline-success pull-xs-right", style: "margin-left:10px"%></li>
      <%= form_tag(users_path, method: :get) do %>
      <%end%>
      <%end%>
      </ul>
    </div>
  </div>
</nav>
  <br>
  <br>
  <div class="container" <%=controller_name%> <%=action_name%>>
    <%= yield %>
  </div>
</body>
</html>
